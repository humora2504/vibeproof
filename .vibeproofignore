# The test fixtures are deliberately vulnerable. They are the test suite,
# not a deployed app, so they must not fail this repository's own CI.
test/fixtures/

# The demo project is deliberately unprotected. It is the demonstration,
# not a deployed app, so it must not fail this repository's own gate.
demo/
